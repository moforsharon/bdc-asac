export interface IPasswordInputProps {
  /**
   * Set input's label.
   */
  label: string;
  labelSize: string;
  placeholder: string;
  /**
   * Set input's error message.
   */
  error?: string;
  className?: string;
}
