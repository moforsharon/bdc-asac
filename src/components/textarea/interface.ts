export interface ITextAreaProps {
  /**
   * Set input's label.
   */
  label: string;
  placeholder: string;
  labelSize: string;
  /**
   * Set input's error message.
   */
  handleEditorChange?: string;
  placeHolder: string;
  error?: string;
  className?: string;
}
